{
  "name": "electron-coin",
  "version": "1.0.0",
  "description": "A simple Electron application",
  "main": "main.ts",
  "scripts": {
    "start": "npm run build && chcp 65001 && electron ./dist/main.js",
    "build": "tsc -p .",
    "watch": "tsc -w",
    "dev": "tsc -p . --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dodola",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/electron": "^1.6.12",
    "@types/node": "^22.13.14",
    "electron": "^35.1.2",
    "typescript": "^5.8.2"
  }
}